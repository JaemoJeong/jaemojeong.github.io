fastapi==0.129.0
uvicorn[standard]==0.41.0
numpy==1.26.4
Pillow==12.2.0
httpx==0.28.1
# Production-only dependencies are intentionally not repinned here. Use the
# exact LanguageBind/SCoPE environment that produced the frozen checkpoints.
